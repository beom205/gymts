<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mt.gmts.mapper.LoginMapper">
	
	<select id="loginUser" parameterType="string" resultType="com.mt.gmts.model.Login">
		SELECT id , password
		 FROM customer
		WHERE id= #{id} 
	</select>
	

</mapper>